import candidateabilities.CandidateAbility;

import java.util.List;

public class CandidateAbilityListBuilder {
    public static List<CandidateAbility> build(CV candidateCV){
        // use of factory and configuration classes and files to load list of candidates abilities
        return list_of_candidates_abilities_check_list;
    }
}
